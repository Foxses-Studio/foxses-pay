{
  "name": "foxses-pay-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/core && npm run build -w packages/bkash && npm run build -w packages/nagad && npm run build -w packages/sslcommerz && npm run build -w packages/stripe && npm run build -w packages/pay",
    "dev": "npm run dev --workspaces --if-present"
  }
}
